###
# Oscillate
#   - data
#   - window
###

script Oscillate {
    input data   = 0;
    input window = 0;

    plot oscillate;
    if window > 1 {
        oscillate = data - Average(data, window);
    }
    else {
        oscillate = data;
    }
}
