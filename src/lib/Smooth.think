script Smooth {
    input value  = 0;
    input length = 0;

    def smoothed;
    if length > 1 {
        smoothed = Inertia(value, length);
    }
    else {
        smoothed = value;
    }

    plot P = smoothed;
    P.Hide();
}
