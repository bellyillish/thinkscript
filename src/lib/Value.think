#include_once "../lib/Chart.think"

script Value {
    input raw = 0;

    def value;
    if !IsNaN(raw) and !IsInfinite(raw) or !Chart().isValidBar {
        value = raw;
    }
    else {
        value = CompoundValue(1, value[1], raw);
    }

    plot P = value;
    P.hide();
}
